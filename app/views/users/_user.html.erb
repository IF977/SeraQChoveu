<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
  | <%= link_to "Deletar Usuário", user, method: :delete,
                                data: { confirm: "Deseja deletar esse usuário?" } %>
  <% end %>
</li>
